import { BooltoonlinePipe } from './booltoonline.pipe';

describe('BooltoonlinePipe', () => {
  it('create an instance', () => {
    const pipe = new BooltoonlinePipe();
    expect(pipe).toBeTruthy();
  });
});
